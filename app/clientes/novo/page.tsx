export const dynamic = "force-dynamic";

export default function NovoClientePage() {
  return (
    <div style={{ padding: 40, color: "white" }}>
      Página Novo Cliente (isolada temporariamente)
    </div>
  );
}